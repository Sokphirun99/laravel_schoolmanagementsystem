#!/usr/bin/env php
<?php

// Suppress PHP 8.4 deprecation warnings when running artisan commands
error_reporting(E_ALL & ~E_DEPRECATED);

// Forward to the actual artisan file
$argv = $_SERVER['argv'];
$_SERVER['argv'] = array_slice($_SERVER['argv'], 0);
require __DIR__.'/artisan';
