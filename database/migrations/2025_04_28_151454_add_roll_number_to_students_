table.php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::table('students', function (Blueprint $table) {
            // Add roll_number column if it doesn't exist
            if (!Schema::hasColumn('students', 'roll_number')) {
                $table->string('roll_number')->nullable()->after('admission_number');
            }

            // Add emergency_contact if missing
            if (!Schema::hasColumn('students', 'emergency_contact')) {
                $table->string('emergency_contact')->nullable()->after('phone');
            }

            // Add medical_condition if missing
            if (!Schema::hasColumn('students', 'medical_condition')) {
                $table->text('medical_condition')->nullable()->after('status');
            }

            // Add school_id if missing
            if (!Schema::hasColumn('students', 'school_id')) {
                $table->unsignedBigInteger('school_id')->nullable()->after('parent_id');
            }

            // Add photo if missing
            if (!Schema::hasColumn('students', 'photo')) {
                $table->string('photo')->nullable()->after('user_id');
            }
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::table('students', function (Blueprint $table) {
            // Drop columns if they exist
            if (Schema::hasColumn('students', 'roll_number')) {
                $table->dropColumn('roll_number');
            }

            if (Schema::hasColumn('students', 'emergency_contact')) {
                $table->dropColumn('emergency_contact');
            }

            if (Schema::hasColumn('students', 'medical_condition')) {
                $table->dropColumn('medical_condition');
            }

            if (Schema::hasColumn('students', 'school_id')) {
                $table->dropColumn('school_id');
            }

            if (Schema::hasColumn('students', 'photo')) {
                $table->dropColumn('photo');
            }
        });
    }
};
